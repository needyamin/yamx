# Copy to .env next to docker-compose.yml and adjust.
#
# Default compose uses qwen2.5-coder:0.5b - very low RAM/CPU footprint.
# If your host still struggles, try an even smaller model in .env.
# If your host still struggles, use a tiny non-coder model instead (for example qwen2.5:0.5b).
#
# OLLAMA_MODEL=qwen2.5-coder:0.5b
# OLLAMA_MODEL=gemma4:e4b
# OLLAMA_MODEL=qwen2.5-coder:3b
# OLLAMA_MODEL=llama3.2:3b

OLLAMA_MODEL=qwen2.5-coder:0.5b

# Optional: if ollama-init keeps failing mid-pull (slow or flaky network)
# OLLAMA_INIT_PULL_RETRIES=12
# OLLAMA_INIT_PULL_RETRY_SECONDS=45
# OLLAMA_INIT_START_DELAY=10
